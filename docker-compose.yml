services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: chatapp-sql
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=YourStrong!Passw0rd
    ports:
      - "1433:1433"
    healthcheck:
      test:
        [
          "CMD",
          "/opt/mssql-tools18/bin/sqlcmd",
          "-No",
          "-U",
          "sa",
          "-P",
          "YourStrong!Passw0rd",
          "-Q",
          "SELECT 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - sql_data:/var/opt/mssql

  redis:
    image: redis:alpine
    container_name: chatapp-redis
    ports:
      - "6379:6379"

  gateway-service:
    build:
      context: ./backend/ChatApp.ApiGateway
    ports:
      - "8080:8080"
    depends_on:
      - auth-service
      - messaging-service
      - realtime-service
      - user-service

  auth-migrator:
    build:
      context: ./backend/ChatApp.Auth
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ChatApp.Auth;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=your-super-secret-key-at-least-32-characters-long!
      - JwtSettings__Issuer=ChatApp.Auth
      - JwtSettings__Audience=ChatApp.Client
      - MIGRATION_JOB=true
    depends_on:
      sqlserver:
        condition: service_healthy
    entrypoint: ["./efbundle"]

  auth-service:
    build:
      context: ./backend/ChatApp.Auth
      dockerfile: Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ChatApp.Auth;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=your-super-secret-key-at-least-32-characters-long!
      - JwtSettings__Issuer=ChatApp.Auth
      - JwtSettings__Audience=ChatApp.Client
    depends_on:
      sqlserver:
        condition: service_healthy
      redis:
        condition: service_started
      auth-migrator:
        condition: service_completed_successfully

  messaging-migrator:
    build:
      context: ./backend/ChatApp.Messaging
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ChatApp.Messaging;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=your-super-secret-key-at-least-32-characters-long!
      - JwtSettings__Issuer=ChatApp.Auth
      - JwtSettings__Audience=ChatApp.Client
      - MIGRATION_JOB=true
    depends_on:
      sqlserver:
        condition: service_healthy
    entrypoint: ["./efbundle"]

  messaging-service:
    build:
      context: ./backend/ChatApp.Messaging
      dockerfile: Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ChatApp.Messaging;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=your-super-secret-key-at-least-32-characters-long!
      - JwtSettings__Issuer=ChatApp.Auth
      - JwtSettings__Audience=ChatApp.Client
    depends_on:
      sqlserver:
        condition: service_healthy
      redis:
        condition: service_started
      messaging-migrator:
        condition: service_completed_successfully

  realtime-service:
    build:
      context: ./backend/ChatApp.RealTime
      dockerfile: Dockerfile
    environment:
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=your-super-secret-key-at-least-32-characters-long!
      - JwtSettings__Issuer=ChatApp.Auth
      - JwtSettings__Audience=ChatApp.Client
    depends_on:
      - redis

  user-migrator:
    build:
      context: ./backend/ChatApp.UserDetails
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ChatApp.UserDetails;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
      - MIGRATION_JOB=true
    depends_on:
      sqlserver:
        condition: service_healthy
    entrypoint: ["./efbundle"]

  user-service:
    build:
      context: ./backend/ChatApp.UserDetails
      dockerfile: Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ChatApp.UserDetails;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=your-super-secret-key-at-least-32-characters-long!
      - JwtSettings__Issuer=ChatApp.Auth
      - JwtSettings__Audience=ChatApp.Client
    depends_on:
      sqlserver:
        condition: service_healthy
      redis:
        condition: service_started
      user-migrator:
        condition: service_completed_successfully

  frontend:
    build:
      context: ./frontend/ChatApp.Frontend
      dockerfile: Dockerfile
      args:
        - VITE_BACKEND_BASE_URL=http://localhost:8080
    ports:
      - "5173:80"
    depends_on:
      - gateway-service

volumes:
  sql_data:
